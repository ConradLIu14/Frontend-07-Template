·计算主轴方向
    ·找出所有Flex元素// flex排版最大的特点就是会产生带有flex 的元素
                    // 遇见flex item 的时候，会自动把当前行填满；如果有多个flex 元素就按（flex 值的，类似打分）比例分配
                    // 如果遇见 剩余为负数的时候；flex item之间距离为零。其他元素等比例压缩
    ·把主轴方向的剩余尺寸按比例分配给这些元素
    ·若剩余空间为负数，所有flex 元素为0，等比压缩剩余元素